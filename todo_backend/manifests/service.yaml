apiVersion: v1
kind: Service
metadata:
  namespace: project
  name: todobackend-svc
spec:
  type: ClusterIP
  selector:
    app: todobackend
  ports:
    - port: 2345
      protocol: TCP
      targetPort: 8080
